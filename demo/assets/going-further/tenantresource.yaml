apiVersion: capsule.clastix.io/v1beta2
kind: TenantResource
metadata:
  name: solar-app-config
  namespace: solar-development
spec:
  serviceAccount:
    name: config-replicator
  resyncPeriod: 10s
  resources:
    - namespaceSelector:
        matchLabels:
          environment: prod
      namespacedItems:
        - apiVersion: v1
          kind: ConfigMap
          namespace: solar-development
          selector:
            matchLabels:
              distribute: solar
